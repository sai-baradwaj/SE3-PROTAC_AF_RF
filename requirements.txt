# SE3AF v3.9.0 — Production Requirements
# =========================================
# Quick install (web app only, no GPU training):
#   pip install -r requirements.txt
#
# Full install (training + inference):
#   pip install -r requirements.txt
#   pip install torch>=2.1.0
#   pip install torch-scatter torch-sparse torch-cluster torch-geometric>=2.4.0
#   pip install fair-esm>=2.0.0
#
# v3.9.0 CHANGES:
#   ADD flask-cors, reportlab, requests — frontend API deps
#   ADD num_workers OS guard (GLOBAL_CONFIG.py)
#   ADD ProteinGeometryEncoder, ProteinBackboneEncoder (core/model.py)
#   ADD af_residue_graph() (core/alphafold.py)
#   FIX scaffold-split data paths (data/split_scaffold/)

# ── Web Framework ─────────────────────────────────────────────────────────
flask>=3.0.0
flask-cors>=4.0.0
werkzeug>=3.0.0

# ── HTTP / Networking ─────────────────────────────────────────────────────
requests>=2.31.0

# ── Report Generation ─────────────────────────────────────────────────────
reportlab>=4.0.0

# ── Chemistry ─────────────────────────────────────────────────────────────
rdkit>=2023.3.1

# ── Data Science ──────────────────────────────────────────────────────────
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.11.0
scikit-learn>=1.3.0
joblib>=1.3.0

# ── Progress Bars ─────────────────────────────────────────────────────────
tqdm>=4.66.0

# ── Core ML (optional — for model training/inference) ─────────────────────
# Uncomment to enable real SE3AF predictions:
torch>=2.1.0
# torch-scatter
# torch-sparse
# torch-cluster
# torch-geometric>=2.4.0

# ── Protein Embeddings (optional) ─────────────────────────────────────────
# ESM-2 (1280-dim) embeddings; falls back gracefully if not installed:
# fair-esm>=2.0.0

# ── Environment ───────────────────────────────────────────────────────────
python-dotenv
gunicorn>=21.2.0
requests>=2.31.0